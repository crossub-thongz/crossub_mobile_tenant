/**
 * @crossub-thongz/api-contract — the CROSSUB mobile API contract.
 *
 * Generated from `openapi.mobile.json` (the /api/v1 facade surface). Import the typed
 * client factory, or the raw `paths` / `components` / `operations` types.
 */
export type { paths, components, operations } from './types';
export { createCrossubClient } from './client';
export type { CrossubClient, CrossubClientOptions } from './client';
