# @crossub-thongz/api-contract is published to GitHub Packages from crossub_web.
# Supply GITHUB_TOKEN (a token with read:packages) via the environment — locally and
# on Render. No literal token is committed here.
@crossub-thongz:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
